# Worklog 2026-08-24 - zaloSALE

## Da lam
- [x] Cai Vibe Coding Kit v1.0 (FULL 28 files) + fix opencode.json (skills: array → object, mcp type)
- [x] Tao skill mattpocock-prompt-master (TS+React+Node+Postgres, 1 hàm/lần, 7 file md, 3 góc nhìn)
- [x] BIZ-001 + SPEC-001 (check tin nhắn Community 3 ngày, fallback 3 tầng) — Approved
- [x] PROMPT-001 (6 khối) + TASK-001 (service.checkCommunity) — Done
- [x] Code `src/features/community-check/service.js` (38 dòng, header 3 Biết) + `service.test.js` (3 case PASS)
- [x] Verify: `npm test` 1429 PASS, `node --check` ok, `npm run build` xanh, `REVIEW-001` Approved

## Hoc duoc
- **Thợ:** File ≤300, hàm ≤50 & 1 việc giúp test dễ; mock `fetchFn` thay vì mock `api` cho test nhanh
- **Kiến trúc:** Tách `src/features/community-check/` riêng thay vì nhét vào `history.js` để sau thêm `route.ts` không phình
- **PM:** Chia 1 feature thành 3 slice (service → route → UI), mỗi slice 2h, P0 trước

## Quyet dinh
- Chọn file local `data/history.jsonl` thay vì Postgres cho slice 1 (nhanh, đã có store) — ghi ADR nếu sau này cần Postgres

## Tiep theo
- [x] TASK-002: `route.ts` POST /api/community/check — Done (22 dòng, 2 test PASS, đã gắn vào server-api.js)
- [x] TASK-003: UI `CommunityCheckPanel.tsx` (138 dòng, thumbnail + calendar 3 ngày, gọi API) — Done (2 test PASS, đã gắn vào App.tsx)
- [x] Verify: `npm test` 1429 PASS, `npm run build` xanh, `REVIEW-003` Approved

## Tiep theo (sau 3 slice)
- Demo E2E: chọn nhóm `[1]...` + 21→24/08 + keyword `cau giay` → Quét → thấy total
- Hoặc thêm forward cho Community nếu cần



