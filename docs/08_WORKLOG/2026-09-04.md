# Worklog — 2026-09-04

## TASK-005 — Kho tin phòng đã gửi

- BIZ-002 và SPEC-002 được user duyệt; chọn SQLite FTS5 qua `node:sqlite`, yêu cầu Node 22.13+.
- Thêm hồ sơ phòng và lịch sử từng lần bot gửi; cùng mã được gom, nguồn/đích vẫn truy vết riêng.
- Thêm API tìm kiếm có validation và panel React tìm mã/địa chỉ không dấu.
- TDD: repository SQLite thật, route, Forwarder integration và UI component.
- Browser: desktop và mobile 390×844 hiển thị dữ liệu mẫu thật từ API/SQLite tạm; dữ liệu test đã xóa.
- Verify: full test + build + type-check PASS; audit còn 1 Moderate `qs`, không có High/Critical.
- Review sửa hai lỗi: DB path sai cấp và phân loại nhầm lỗi ghi DB thành lỗi gửi Zalo.

## Bài học thợ code / kiến trúc

Tách Route → Service → Repository giúp test quy tắc gom phòng mà không nhét SQL vào Forwarder. Callback chỉ chạy sau khi Zalo xác nhận gửi để “đã gửi” phản ánh đúng sự kiện thực tế.

## Fix — Không loading cấu hình vô hạn

- Nguyên nhân: Vite 5173 hoạt động nhưng backend 3000 tắt; `App` bắt lỗi rồi giữ `config=null` mãi.
- Sửa: thêm trạng thái lỗi có hướng dẫn `npm start` và nút `Thử lại`; loading chỉ dùng khi request đang chờ.
- TDD: thêm regression test lỗi lần đầu, retry thành công; browser 5173 hiển thị đúng cảnh báo.
- Verify: 1.488 backend cases + 9 React tests PASS; production build/type-check PASS.

## TASK-006 — Hoa hồng và xem ảnh trong bảng quét

- Thêm `commissionPercent` từ dòng có `🌹`, `HH` hoặc `hoa hồng`; UI hiển thị cột `%`.
- Trích tối đa 8 URL ảnh `http/https`; UI hiện tối đa 3 thumbnail và lightbox đóng bằng nút/ESC.
- TDD gồm parser, URL không an toàn và hành vi mở/đóng ảnh.
- Sửa regression từ ảnh thực tế: ký hiệu `💐 30%12th` nay được nhận là hoa hồng `30%`.
- Sửa tin ảnh Community: đọc `href/thumb` trong object `content`, ưu tiên URL ảnh gốc thay vì thumbnail.
- Sửa thứ tự gom: không còn đảo mảng phản hồi; luôn stable-sort theo timestamp tăng dần trước khi chạy state machine.
- Album ảnh Community được nhận diện bằng cùng module media và không bị cắt khỏi cụm đang mở.
- Ranh giới cụm: mọi chữ/media sau opener ở lại cụm cho tới opener tiếp theo; ảnh trước opener mới không bị kéo sang cụm mới.
- Forward gửi tuần tự theo đơn vị chữ/album; các ảnh liên tiếp vẫn là một album và dùng cùng nhóm đích của cụm.
- Thêm `Xem cụm`: bảng giữ tóm tắt, dialog hiển thị toàn bộ phần chữ/album theo thứ tự để kiểm tra mã phòng trước khi gửi.
- Verify: 1.503 backend cases + 10 React tests, production build/type-check PASS.
- Review: cần restart backend hiện tại trước khi kiểm tra dữ liệu Zalo thật; URL ảnh Zalo có thể hết hạn.

## Bài học TASK-006

Hoa hồng phải đọc từ nội dung gốc trước bước xóa dòng phần trăm. Tách bộ trích URL media thành module dùng chung giúp preview và forward áp dụng cùng một quy tắc an toàn.
