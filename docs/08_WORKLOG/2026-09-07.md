<!--
Ai viết: Codex
Tại sao: Ghi evidence cho lát sticker-separated để lần sau biết phạm vi và rủi ro còn lại.
Link: docs/05_TASKS/TASK-007_StickerSeparatedPosts.md
-->

# Worklog — 2026-09-07

## TASK-007

- Ưu tiên sticker làm ranh giới cho mọi nhóm nguồn; sticker không được forward.
- Khi tin dài mở cụm xuất hiện sau ảnh/nhãn, chỉ tin đó được sắp lên đầu cụm trước khi forward; nhãn phòng và ảnh vẫn xen kẽ theo đúng thứ tự nguồn.
- Regression test PASS cho `P101 → ảnh → P102 → ảnh → mở cụm` và trường hợp chỉ một phòng trước tin mở cụm.
- Footer regression PASS: nhiều delivery unit vẫn kết thúc bằng đúng sticker người dùng đã set.
- `npm test`, `npm run build`, `git diff --check` đều PASS.
- Không chạy Zalo thật để tránh gửi tin hoặc dùng phiên đăng nhập của user; cần một lần quét/forward thủ công vào nhóm test để xác nhận payload thực tế.
