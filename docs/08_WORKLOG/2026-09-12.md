<!--
Ai viết: Codex
Tại sao: Lưu evidence và rủi ro của foundation dữ liệu bản đồ để các slice sau không lặp lại quyết định.
Link: docs/05_TASKS/TASK-008_MapSearchRequestFoundation.md
-->

# Worklog — 2026-09-12

## TASK-008

- Thêm customer-search cục bộ: khách có số điện thoại chuẩn hóa duy nhất, nhiều nhu cầu tìm trọ và bật/tắt từng nhu cầu.
- Luồng code là `route → service → repository → SQLite`; lỗi nội bộ không được trả về client và feature không ghi PII vào log.
- TDD dùng SQLite thật chứng minh migration, ràng buộc trùng số và quan hệ một khách–nhiều nhu cầu.
- Sửa hai lỗi nền đã được user cho phép: fixture test thiếu `groupLink/history`, và `onClick` truyền `MouseEvent` vào hàm nhận danh sách chỉ số.
- `npm test` (1,641 script cases + 43 React tests), `npm run build`, `git diff --check` pass. Root không có `tsconfig`; `npx tsc --noEmit` chỉ hiện trợ giúp, trong khi type-check hợp lệ nằm trong build frontend.
- Không chạy Zalo thật hay geocoding; chưa thêm dependency. `npm audit --omit=dev` còn một Moderate ở `qs`, không có High/Critical.

## Bài học thợ code / kiến trúc

- `phone_normalized` là khóa nghiệp vụ để phát hiện trùng, còn `phone_display` giữ cách người vận hành đã nhập; hai trách nhiệm không nên gộp vào một cột.
- Tách route/service/repository giúp test SQLite thật mà không cần chạy Zalo hoặc UI, và giữ phần map sau này không lẫn vào luồng khách.

## TASK-009

- Thêm vùng tìm trọ local: nhãn, tọa độ tâm, bán kính 200–20.000m và trạng thái bật/tắt; tối đa 10 vùng cho mỗi nhu cầu.
- API CRUD vùng kiểm tra ownership và trả lỗi an toàn; database cũng có unique constraint chống trùng tâm+bán kính trong cùng nhu cầu.
- Tách Haversine thành hàm thuần, dùng mét xuyên suốt. Một phòng khớp nhiều vùng chỉ xuất hiện một lần cùng danh sách zone IDs.
- Focused tests, `npm test` (1,641 script cases + 43 React tests), `npm run build` và `git diff --check` pass.

## Bài học thợ code / kiến trúc — TASK-009

- Haversine không biết SQLite hay HTTP nên test biên khoảng cách nhanh và ổn định; service chỉ quyết định quota/ownership.

## TASK-010

- Thêm tọa độ và trạng thái định vị cho `room_records`, cache Nominatim local và migration idempotent cho DB cũ.
- Endpoint `POST /api/rooms/:roomId/location/resolve` nhận duy nhất địa chỉ phòng; located chỉ khi có đúng một candidate, ambiguous/not_found/failed không tạo marker.
- Geocoder có User-Agent, tuần tự 1 request/giây, timeout 10 giây, giới hạn phản hồi 1 MB; mock tests chứng minh không gửi dữ liệu khách/Zalo.
- `npm test`, `npm run build`, `git diff --check` pass; audit còn 1 Moderate `qs`, không có High/Critical.

## Bài học thợ code / kiến trúc — TASK-010

- Cache theo địa chỉ chuẩn hóa giảm tải provider và làm retry ổn định; trạng thái `ambiguous` tách rõ “có kết quả nhưng chưa an toàn để đặt marker”.
- Ranh giới `geocoder → service → repository → route` cho phép thay provider mà không đổi luật stale hoặc schema HTTP.

## TASK-011

- Thêm API geocode/map-rooms và UI `MapSearchPanel` + `LeafletMap`: tìm/click tâm, bán kính, nhiều vùng, marker phòng khớp và ba danh sách trạng thái.
- Leaflet/React Leaflet được lazy-load; chunk map tách khỏi bundle màn quét. Attribution OpenStreetMap hiển thị trực tiếp trên map.
- UI tests mock tile/map (46 React tests toàn bộ pass); browser smoke desktop/mobile xác nhận panel và attribution; build pass.

## Bài học thợ code / kiến trúc — TASK-011

- Tách `MapSearchPanel` (state/API) khỏi `LeafletMap` (render bản đồ) giúp test form không phụ thuộc DOM/tile thật.
- Phân nhóm matched/unmatched/unlocated ở backend trước khi render giúp UI không vô tình vẽ marker cho tọa độ chưa xác minh.

## Điều chỉnh sau phản hồi người dùng

- Bỏ toàn bộ bước chọn khách/tạo nhu cầu khỏi `MapSearchPanel`; bản đồ chỉ nhận danh sách vùng phiên hiện tại.
- Thêm `POST /api/map/rooms`, giới hạn 10 vùng, và tách thông báo lỗi geocode khỏi lỗi tải phòng.
- Browser smoke với backend thật: thêm vùng thành công, không còn ô khách hoặc nhu cầu; Nominatim không truy cập được do DNS nên UI báo đúng lỗi và vẫn cho phép click map.
- Bổ sung fallback Photon khi Nominatim lỗi; parser GeoJSON chỉ giữ tọa độ hợp lệ và vẫn giới hạn phản hồi 1 MB.
- Thêm nút chọn nhanh 3 cụm Ga Hà Đông/Mỗ Lao/Phùng Khoang với tâm và bán kính mặc định để thao tác không phụ thuộc geocoder.
- Thêm nút định vị từng tin pending bằng địa chỉ đã lưu; sau khi thành công tự tải lại danh sách vùng.
- Tin thiếu địa chỉ có ô nhập trực tiếp trong danh sách, tránh nút Định vị bị khóa.
- Nối bước quét tin với bộ tách địa chỉ; backfill từ nội dung gốc đã gửi đã cập nhật 136 hồ sơ cũ có địa chỉ nhận diện được.
